function enemyAttack() {
  const distance = camera.position.distanceTo(enemy.position);

  if (distance < 8) {
    damagePlayer(1); // small damage over time
  }
}
// Run enemy attack loop
setInterval(enemyAttack, 200);
}
  }
